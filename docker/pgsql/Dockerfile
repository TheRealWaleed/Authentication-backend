FROM postgres

ARG DATABASE_NAME

ARG DATABASE_USER

ARG DATABASE_PASSWORD

ENV POSTGRES_DB $DATABASE_NAME

ENV POSTGRES_USER $DATABASE_USER

ENV POSTGRES_PASSWORD $DATABASE_PASSWORD

COPY ./docker/pgsql/install-uuid-extension.sh /docker-entrypoint-initdb.d/
