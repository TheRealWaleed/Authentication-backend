const describe = require('mocha').describe;
const assert = require('chai').assert;

describe ('Authentication', () => {
    describe('register', () => {
        it('should be invalid if parameters are wrong', (req,res) => {

        })
    });
});
